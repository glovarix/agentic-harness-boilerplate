# Your workflow

Edit this file. The agent reads it every session after `context/instructions/`.

## Job loop

1. Read README **Your harness** + context/instructions/
2. Inspect context/sources/ — copy to workspace before use
3. Draft in workspace/working/
4. Confirm (preferences.confirmBeforeElevate)
5. Publish to outputs/
6. Sanity report alongside deliverable

## Classification

Add your request types below. First match wins.

| Signal | Action |
| --- | --- |
| | |

## Capabilities

What this harness does once configured:

- orient, classify, evidence-check
- draft → elevate → report
- handoff across sessions
- wiring smoke test

## Scripts

When work is repetitive, add scripts/ and document here. Tweak → fork → create. Never silent overwrite.
